<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- DEBUG BUILD: Allow cleartext for local development -->
    <!-- This permits HTTP connections to local network services like Home Assistant -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
